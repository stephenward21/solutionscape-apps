appId: com.solutionscape.ai-governance
productName: SolutionScape AI Governance
copyright: Copyright © 2025 SolutionScape

# Electron entry point (compiled from electron/main.ts)
extraMetadata:
  main: electron-dist/electron/main.js

directories:
  output: dist-electron
  buildResources: assets

# Files to package — standalone Next.js output + compiled electron process
files:
  - electron-dist/**
  - assets/**
  - package.json

extraResources:
  - from: .next/standalone
    to: .next/standalone
  - from: .next/static
    to: .next/standalone/.next/static
  - from: public
    to: .next/standalone/public

# Native modules (better-sqlite3) cannot live inside asar — unpack them
asarUnpack:
  - "**/*.node"
  - "**/better-sqlite3/**"

# macOS
mac:
  category: public.app-category.business
  icon: assets/icon.icns
  hardenedRuntime: true
  gatekeeperAssess: false        # notarytool handles this; skip local check
  entitlements: assets/entitlements.mac.plist
  entitlementsInherit: assets/entitlements.mac.plist
  notarize:
    teamId: ${APPLE_TEAM_ID}     # 10-char Team ID from developer.apple.com
  target:
    - target: dmg
      arch: [x64, arm64]
    - target: zip
      arch: [x64, arm64]

dmg:
  title: SolutionScape AI Governance

# Windows
win:
  icon: assets/icon.ico
  # Certificate loaded from WIN_CSC_LINK (.pfx) + WIN_CSC_KEY_PASSWORD env vars.
  # For EV certs on a hardware token, set WIN_CSC_LINK to the cert thumbprint and
  # signtool will pick it up from the token — do not set WIN_CSC_KEY_PASSWORD.
  publisherName: SolutionScape        # must match the O= field in your certificate
  signingHashAlgorithms: [sha256]
  target:
    - target: nsis
      arch: [x64, arm64]

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  # Show license on install
  license: assets/LICENSE.rtf
  # Installer branding
  installerIcon: assets/icon.ico
  uninstallerIcon: assets/icon.ico

# Linux
linux:
  icon: assets/icon.png
  category: Office
  target:
    - target: AppImage
      arch: [x64]
